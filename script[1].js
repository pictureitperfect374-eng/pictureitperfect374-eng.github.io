const menuToggle=document.querySelector('.menu-toggle');const navLinks=document.querySelector('.nav-links');menuToggle.addEventListener('click',()=>{const open=navLinks.classList.toggle('open');menuToggle.setAttribute('aria-expanded',String(open))});document.querySelectorAll('.nav-links a').forEach(a=>a.addEventListener('click',()=>{navLinks.classList.remove('open');menuToggle.setAttribute('aria-expanded','false')}));document.getElementById('year').textContent=new Date().getFullYear();
const range=document.getElementById('compareRange'),afterPane=document.getElementById('afterPane'),sliderLine=document.getElementById('sliderLine');range.addEventListener('input',()=>{afterPane.style.width=`${range.value}%`;sliderLine.style.left=`${range.value}%`});
document.querySelectorAll('[data-service]').forEach(link=>link.addEventListener('click',()=>{document.getElementById('serviceSelect').value=link.dataset.service}));
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
function handleSubmit(event){event.preventDefault();const d=new FormData(event.target);const subject=encodeURIComponent(`Free Estimate Request - ${d.get('service')}`);const body=encodeURIComponent(`Name: ${d.get('name')}\nPhone: ${d.get('phone')}\nEmail: ${d.get('email')}\nProject Location: ${d.get('location')}\nService: ${d.get('service')}\n\nProject Details:\n${d.get('message')}`);window.location.href=`mailto:pictureitperfect374@gmail.com?subject=${subject}&body=${body}`;return false}
